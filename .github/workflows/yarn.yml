name: CI
on: [push]
jobs:
  build:
    name: Test
    runs-on: [macos-latest]
    steps:
      - uses: actions/checkout@v1
      - uses: borales/actions-yarn@v2.1.0
        with:
          cmd: install
      - uses: borales/actions-yarn@v2.1.0
        with:
          cmd: dist
